野良&探し
&[^(amp)|^(lt)|^(gt)|^(nbsp)|^(emsp)|^(copy)|^(reg)|^\#]

ルート絶対パスor絶対URL以外のリンク
href=\"[^\/|^h]

圧縮対象の1行CSSを探す
\{\r\n\t.{1,}\r\n\}

色指定に小文字が入っていないか探す
#[0-9|a-f]{6}

1行の中で2つ条件がある場合
対象が行の場合
^(.*foo.*|.*bar.*|.*baz.*|.*qux.*)$

対象が文言のみの場合
(foo|bar|baz|qux)


